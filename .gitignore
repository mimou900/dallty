# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Vercel build output / local CLI state
.vercel

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Supabase CLI local link cache (project ref, pooler URL, version info)
supabase/.temp/

# Secrets
.env
.env.*
!.env.example

# Local MCP server config (contains the 21st.dev Magic API key)
.mcp.json

# Scratch output from scripts/generate-*-seed.mjs -- review before turning
# into a real migration, never auto-committed.
scripts/output/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
